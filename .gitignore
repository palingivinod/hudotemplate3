node_modules
dist
shots
.DS_Store
*.log
.env
.env.*

# Source frame archives (site uses public/hodu-frames; avoid duplicating ~412MB)
hodu_frames.zip
hodu_frames2.zip
*.zip

# Generated lite tier — rebuilt by scripts/build-assets.mjs on npm run frames
public/hodu-frames-lite
public/hodu-frames/manifest.json

# Keep public/stills in git (needed for Netlify; build can refresh them)
